# OS-Jackfruit
A mini container runtime with kernel-level memory monitoring using engine.c and monitor.c. The project supports container creation, supervision, logging, scheduling, and cleanup, while the kernel module tracks memory usage, raises soft-limit warnings, and kills containers that exceed hard memory limits.
